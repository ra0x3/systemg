version: "2"
projects:
  demo:
    name: Demo
    services:
      db:
        command: "sleep 3000"
      api:
        command: "sleep 3000"
        depends_on: [db]
      worker:
        command: "sleep 3000"
