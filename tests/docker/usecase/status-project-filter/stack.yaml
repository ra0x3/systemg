version: "2"
projects:
  alpha:
    name: Alpha
    services:
      web:
        command: "sleep 3000"
      worker:
        command: "sleep 3000"
  beta:
    name: Beta
    services:
      web:
        command: "sleep 3000"
      worker:
        command: "sleep 3000"
