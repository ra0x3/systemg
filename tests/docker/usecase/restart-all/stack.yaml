version: "2"
projects:
  demo:
    name: Demo
    services:
      svc1:
        command: "sleep 3000"
        restart_policy: "always"
      svc2:
        command: "sleep 3000"
        restart_policy: "always"
