version: "2"
projects:
  alpha:
    name: Alpha
    services:
      a1:
        command: "sleep 3000"
        restart_policy: "always"
      a2:
        command: "sleep 3000"
        restart_policy: "always"
  beta:
    name: Beta
    services:
      b1:
        command: "sleep 3000"
        restart_policy: "always"
