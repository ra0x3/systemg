version: "1"
projects:
  shop:
    name: Shop
    services:
      api:
        command: "sleep 3000"
        restart_policy: "always"
      worker:
        command: "sleep 3000"
        restart_policy: "always"
        skip: true
