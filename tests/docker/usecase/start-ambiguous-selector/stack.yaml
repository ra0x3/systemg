version: "1"
projects:
  alpha:
    name: Alpha
    services:
      worker:
        command: "sleep 3000"
        restart_policy: "always"
        skip: true
  beta:
    name: Beta
    services:
      worker:
        command: "sleep 3000"
        restart_policy: "always"
        skip: true
